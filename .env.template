NRP_CORE_TAG=local
# Local build namespace produced by build_nrp_core_image.sh (default).
# A fresh host has no images published yet, so keep this until CI ships them.
NRP_DOCKER_REGISTRY=nrp-local
# Published variants (Docker Hub) once available:
# NRP_DOCKER_REGISTRY=hbpneurorobotics
