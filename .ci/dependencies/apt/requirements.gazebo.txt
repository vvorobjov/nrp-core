# Gazebo 11 Classic and ROS 2 packages are installed by
# dockerfiles/gazebo.Dockerfile. The gazebo11/gazebo/plugin-base
# spellings differ between Ubuntu 20.04 (OSRF repo) and 22.04 (jammy
# universe), so they are handled in the Dockerfile's RUN with a case
# on lsb_release -cs rather than via this file. Left here as a stub.
